const {describe, it} = require('mocha')
const {expect} = require('chai')
//const student = require('../../src/pages/admin.ts')
//const student = require('../../src/pages/student.ts')

describe('admin', function () {

  describe('constructor', function () {
      it('should call refreshData', function () {

      })
  })

  describe('refreshData', function () {
    it('should call get and subscribe for the response', function () {

    })
    it('should push all transactions from the response into an array', function () {

    })
    it('should call the timeout to refresh the list every 2 seconds', function () {

    })
  })

    it('Sample description', function () {
      })
})